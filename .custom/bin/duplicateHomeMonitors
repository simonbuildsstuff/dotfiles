#!/bin/bash

primary=eDP1
externalMonitor="$(xrandr | grep " connected" | grep $primary -v | cut -f1 -d " ")"

xrandr --output $primary --mode 1920x1080 --scale 1x1 --output $externalMonitor --same-as $primary --mode 1920x1080 --scale 1x1
